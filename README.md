# Vision-Transformer-for-Human-Activity-Recognition
The Vision Transformer (ViT) model, traditionally used for image classification, will be fine-tuned to recognize actions from short video clips (from HMDB Dataset). By learning patterns across frames, the model will distinguish among multiple action classes.
